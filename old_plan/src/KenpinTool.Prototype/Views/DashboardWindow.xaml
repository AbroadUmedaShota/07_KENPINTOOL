<Window x:Class="KenpinTool.Prototype.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:KenpinTool.Prototype"
        xmlns:conv="clr-namespace:KenpinTool.Prototype.Converters"
        mc:Ignorable="d"
        Title="KENPINTOOL ダッシュボード" Height="450" Width="800"
        WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <conv:CountToVisibilityConverter x:Key="CountToVisConverter"/>
    </Window.Resources>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="案件一覧" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"/>
            <Button Content="新規案件作成" Command="{Binding CreateCaseCommand}" Padding="10,5" />
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Cases}" SelectedItem="{Binding SelectedCase}"
                  AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                  CanUserAddRows="False" CanUserDeleteRows="False" HeadersVisibility="Column"
                  RowDetailsVisibilityMode="VisibleWhenSelected">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="案件名" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="入力パス (代表)" Binding="{Binding InputPath}" Width="*"/>
                <DataGridTextColumn Header="ステータス" Binding="{Binding Status}" Width="80"/>
                <DataGridTextColumn Header="開始日時" Binding="{Binding OpenedAtUtc, StringFormat='yyyy/MM/dd HH:mm'}" Width="140"/>
            </DataGrid.Columns>
            <DataGrid.RowDetailsTemplate>
                <DataTemplate>
                    <Border Background="#F9F9F9" Padding="10" BorderBrush="#DDD" BorderThickness="0,1,0,0">
                        <StackPanel>
                            <TextBlock Text="読み込み済みフォルダ:" FontWeight="Bold" Margin="0,0,0,5"/>
                            <ItemsControl ItemsSource="{Binding Folders}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,2">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="📂" Margin="0,0,5,0"/>
                                            <TextBlock Grid.Column="1">
                                                <Run Text="{Binding Path, Mode=OneWay}"/>
                                                <Run Text=" (" Foreground="Gray"/>
                                                <Run Text="{Binding PageCount, Mode=OneWay}"/>
                                                <Run Text=" ページ)" Foreground="Gray"/>
                                            </TextBlock>
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock Text="フォルダなし (空の案件)" 
                                       Visibility="{Binding Folders.Count, Converter={StaticResource CountToVisConverter}}"
                                       Foreground="Gray" FontStyle="Italic"/>
                            
                            <Button Content="＋ フォルダを追加" 
                                    Command="{Binding DataContext.AddFolderToCaseCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    HorizontalAlignment="Left" Margin="0,10,0,0" Padding="10,2" Background="#EEF"/>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </DataGrid.RowDetailsTemplate>
            <DataGrid.InputBindings>
                <MouseBinding Gesture="LeftDoubleClick" Command="{Binding ResumeCaseCommand}" />
            </DataGrid.InputBindings>
        </DataGrid>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" Foreground="Gray" VerticalAlignment="Center"/>
            <Button Grid.Column="1" Content="選択した案件を再開" Command="{Binding ResumeCaseCommand}" Padding="20,10" FontWeight="Bold"/>
        </Grid>
    </Grid>
</Window>
