# KENPINTOOL — AGENTS.md

本リポジトリでCodex（コーディング支援エージェント）が作業する際の、前提・方針・優先順位を定義します。

---

## 1. プロジェクト概要（何を作るか）

- 電子化業務の「検品」を **判断支援（ルール強制）** により高速化・均一化するWindows向け自動検品ツール。
- 完全自動化ではなく、**検知（ツール）** と **確定判断（人）** を分離する。

---

## 2. 参照ドキュメント（ソース・オブ・トゥルース）

実装時の正は以下（統合版の記載に従う）。

- [ドキュメント目次](doc/README.md)
- 企画: `doc/01_企画/企画書.md`
- 要件: `doc/02_要件/要件定義書.md` / `doc/02_要件/業務要件定義書.md` / `doc/02_要件/機能要件定義書.md` / `doc/02_要件/非機能要件定義書.md`
- 統制: `doc/05_統制/NG定義・判断基準マスタの具体化.md`
- UI: `doc/03_UI/デザイン要件定義書.md` / `doc/03_UI/UI詳細設計書.md`
- 技術・PoC: `doc/04_技術/技術選定.md` / `doc/04_技術/プロトタイプ計画.md`

---

## 3. 非交渉の制約（破ってはいけない）

- **外部通信なし**（アップデート、テレメトリ、API送信を含む）  
  ※開発時の依存追加は可だが、実行時に外部へ通信しないこと。
- **裁量判断の排除**：NG理由はNGコードで管理し、自由記述のみでの登録は不可。
- **NG-Aは例外承認不可**（特に QLT-05 は「再スキャンのみ」）。
- **再現性**：同一入力＋同一ルールなら同一結果（ログ/集計/出力が追跡可能）。
- **案件処理中のルール変更禁止**（案件に紐づくルールセットを固定する）。

---

## 4. 実装の優先順位（迷ったらこれ）

1) UIスループット（キーボード完結、3ペイン、比較、ズーム/パン、フォーカス管理）  
2) ログ/監査（追記型＋改ざん検知、疑い→確定の履歴）  
3) 入出力（PDF/JPEG投入、CSV/レポート出力）  
4) 検知アルゴリズム（Phase 1の対象コードから段階導入）

詳細な実装順は `doc/04_技術/プロトタイプ計画.md` と `doc/04_技術/技術選定.md` を参照。

---

## 5. リポジトリ構成

- `doc/`: 企画・要件・UI・技術選定・PoC計画（目次: `doc/README.md`）
- `project/`: 引継ぎ資料・テンプレート等の管理資産
- `samples/`: サンプル入力（`samples/inputs/` はGit管理外）
- `src/`: アプリ実装（現状はWPFプロトタイプを含む）
- `tools/`: ローカル補助スクリプト（サンプル生成など）

---

## 6. コーディング規約（.NET / WPF）

- **MVVM優先**：UIロジックはViewModelへ。コードビハインドは「イベントの橋渡し」程度に限定。
- **Nullable有効**：警告は潰す。例外は理由が説明できる場合のみ。
- **依存を増やしすぎない**：閉域運用・配布を考え、まず標準機能＋最小ライブラリで成立させる。
- **性能劣化を避ける**：大判/多ページ前提。UIスレッドで重い処理をしない（非同期/キャッシュ）。
- **出力はUTF-8**：CSV/JSON等はBOMなしUTF-8を基本。
- **仕様の強制はコードで**：NG-A例外不可、QLT-05は「再スキャンのみ」等の統制は、UI/ドメイン層で破れない形にする。

---

## 7. 実行（プロトタイプ）

プロトタイプは `src/KenpinTool.Prototype` にあります。

```powershell
python tools/generate_sample_images.py samples/inputs
dotnet run --project src/KenpinTool.Prototype -- ".\\samples\\inputs"
```

---

## 8. AI駆動開発（前提の使い分け）

本プロジェクトはAI駆動開発を取り入れており、主に **Gemini / Codex / GPT-5.2** を役割分担して利用する。

### 8.1 役割

- **GPT-5.2（相談役）**: 要件解釈の論点整理、設計方針の妥当性レビュー、リスク/トレードオフの言語化、意思決定支援。
- **Gemini（デザイン）**: UI/UX案、情報設計、ワイヤー/配色/視認性の観点レビュー（長時間作業・スループット重視前提）。
- **Codex（実装）**: 幅広い実装作業（プロトタイピング、リファクタ、テスト、ビルド、ドキュメント整備）。

### 8.2 運用ルール（Codexが守ること）

- 設計の不確実性が高い場合は、実装を急がず **GPT-5.2に相談すべき論点**（選択肢/判断基準/未確定事項）を短く提示する。
- UI/UXに影響が大きい変更（画面構成、配色、操作フロー、ショートカット体系）は、**Geminiでのレビュー前提**として差分と意図を明確にする。
- いずれのAI利用でも、プロダクトの制約（外部通信なし、裁量排除、監査性など）は必ず優先し、要件と矛盾する提案は採用しない。

### 8.3 相談のトリガ（迷ったらここを使う）

- **GPT-5.2へ相談**: 要件が曖昧／複数解釈がある、重大な設計変更、監査ログ/改ざん防止の方式、ルールセット固定の設計、性能KPIの定義。
- **Geminiへ相談**: 3ペインの情報設計、色/コントラスト/ハイライト、フォーカス遷移、ショートカット体系、ダークモード方針、長時間作業の疲労軽減。
- **Codexで実装を進める**: 仕様が確定しているUI実装、データモデル、入出力、ログ/CSV、テスト、ビルド手順、ドキュメント整備。

### 8.4 AI利用時の成果物（最低限）

- **「何を決めたか」**（採用案/非採用案/理由）を短く残す（PR本文、または `doc/` に追記）。
- UI/UX変更は、**操作手順（ショートカット含む）**と**制約（NG-A/QLT-05等）**がどう担保されるかを明記する。
- PoC/プロトタイプでは「動くこと」だけでなく、`doc/04_技術/プロトタイプ計画.md` の観点で **評価可能な形**（ログ/出力/再現手順）にする。

### 8.5 依頼の書き方（最短で進めるための情報）

- 目的（何を良くしたいか）と受入条件（Doneの定義）
- 対象の画面/機能/NGコード（例: `QLT-05` / `STR-01S`）
- 入力データの前提（PDF/JPEG、ページ規模、例外パターン）
- 影響してよい範囲（UIだけ/ログ形式も変更可/既存互換維持 など）

---

## 9. Codexスキル運用（必要時のみ）

- ユーザーがスキル名（`$SkillName` または平文）を指定した場合、または依頼がスキルの説明に明確に合致する場合は、そのスキルの `SKILL.md` に従う。
- 参照は「必要最小限」。関連フォルダを丸ごと読み込まず、リンク先を必要な分だけ読む。
- スキル作成/更新が目的なら `skill-creator`、スキルのインストールが目的なら `skill-installer` を優先。

---

## 10. 進め方（Git / PRのベストプラクティス）

- 作業は原則ブランチで行い、`main` へはPRでマージする。
- PRには最低限以下を含める。
  - Summary（何を変えたか）
  - Why（要件/課題に対して何を解決したか）
  - How to verify（ビルド/実行/確認手順）
  - 影響範囲（UI/ログ/データ形式/互換性）
- 仕様/運用統制に関わる変更は、関連ドキュメント（`doc/`）の更新を同梱する。

---

## 11. Definition of Done（最低限の品質ゲート）

- 変更内容に対応する「根拠」が `doc/` かPR本文で追える
- ローカルで `dotnet build` が通る（該当する場合）
- UI変更はショートカット/フォーカス/制約が破れていない
- ログ/CSVなどの出力形式変更は、サンプルと読み手（運用/監査）観点の説明がある
- 外部通信を増やしていない（実行時）
