# KENPINTOOL — AGENTS.md

本リポジトリでCodex（コーディング支援エージェント）が作業する際の、前提・方針・優先順位を定義します。

---

## 1. プロジェクト概要（何を作るか）

- 電子化業務の「検品」を **判断支援（ルール強制）** により高速化・均一化するWindows向け自動検品ツール。
- 完全自動化ではなく、**検知（ツール）** と **確定判断（人）** を分離する。

---

## 2. 参照ドキュメント（ソース・オブ・トゥルース）

実装時の正は以下（統合版の記載に従う）。

- [ドキュメント目次](doc/README.md)
- 企画: `doc/01_企画/企画書.md`
- 要件: `doc/02_要件/要件定義書.md` / `doc/02_要件/業務要件定義書.md` / `doc/02_要件/機能要件定義書.md` / `doc/02_要件/非機能要件定義書.md`
- 統制: `doc/05_統制/NG定義・判断基準マスタの具体化.md`
- UI: `doc/03_UI/デザイン要件定義書.md` / `doc/03_UI/UI詳細設計書.md`
- 技術・PoC: `doc/04_技術/技術選定.md` / `doc/04_技術/プロトタイプ計画.md`

---

## 3. 非交渉の制約（破ってはいけない）

- **外部通信なし**（アップデート、テレメトリ、API送信を含む）  
  ※開発時の依存追加は可だが、実行時に外部へ通信しないこと。
- **裁量判断の排除**：NG理由はNGコードで管理し、自由記述のみでの登録は不可。
- **NG-Aは例外承認不可**（特に QLT-05 は「再スキャンのみ」）。
- **再現性**：同一入力＋同一ルールなら同一結果（ログ/集計/出力が追跡可能）。
- **案件処理中のルール変更禁止**（案件に紐づくルールセットを固定する）。

---

## 4. 実装の優先順位（迷ったらこれ）

1) UIスループット（キーボード完結、3ペイン、比較、ズーム/パン、フォーカス管理）  
2) ログ/監査（追記型＋改ざん検知、疑い→確定の履歴）  
3) 入出力（PDF/JPEG投入、CSV/レポート出力）  
4) 検知アルゴリズム（Phase 1の対象コードから段階導入）

詳細な実装順は `doc/04_技術/プロトタイプ計画.md` と `doc/04_技術/技術選定.md` を参照。

---

## 5. リポジトリ構成

- `doc/`: 企画・要件・UI・技術選定・PoC計画（目次: `doc/README.md`）
- `project/`: 引継ぎ資料・テンプレート等の管理資産
- `samples/`: サンプル入力（`samples/inputs/` はGit管理外）
- `src/`: アプリ実装（現状はWPFプロトタイプを含む）
- `tools/`: ローカル補助スクリプト（サンプル生成など）

---

## 6. コーディング規約（.NET / WPF）

- **MVVM優先**：UIロジックはViewModelへ。コードビハインドは「イベントの橋渡し」程度に限定。
- **Nullable有効**：警告は潰す。例外は理由が説明できる場合のみ。
- **依存を増やしすぎない**：閉域運用・配布を考え、まず標準機能＋最小ライブラリで成立させる。
- **性能劣化を避ける**：大判/多ページ前提。UIスレッドで重い処理をしない（非同期/キャッシュ）。
- **出力は UTF-8 (BOMあり)**: CSV/JSON 等のファイル出力は、Excel 等の国内一般ツールでの視認性を優先し、BOM 付与を基本とする。
- **仕様の強制はコードで**：NG-A例外不可、QLT-05は「再スキャンのみ」等の統制は、UI/ドメイン層で破れない形にする。
  - **UI実装時の絶対ルール**:
    - **日本語表記の遵守**: UI 上のラベル、ボタン名、ステータス表示、メッセージ等はすべて日本語とする（国内利用への最適化）。
    - NG-A（特に QLT-05）に対して「OK」や「例外承認」を選択できる UI 要素を絶対に作成しないこと。
    - 自由記述のコメント欄は、NGコード選択を代替するものであってはならない。
- **設計標準（PoC時点）**:
  - **非同期解析パイプライン**: `Channel<T>` でロード後に検知を逐次実行し、UI更新はUIスレッドへ戻す。
  - **PDF対応**: `PdfiumViewer` を用い、ページ単位で `PageItem` に展開する。
  - **画像解析**: `OpenCvSharp4` を用い、`QualityDetectionService` などで実検知を行う。
  - **重複検知**: `StructureDetectionService` がハッシュを算出し、既知ページと比較する。

---

## 7. 実行（プロトタイプ）

プロトタイプは `src/KenpinTool.Prototype` にあります。

```powershell
python tools/generate_sample_images.py samples/inputs
dotnet run --project src/KenpinTool.Prototype -- ".\\samples\\inputs"
```

---

## 8. AI駆動開発（Codex × Gemini CLI 併用）

本プロジェクトはAI駆動開発を取り入れており、基本運用は **Codex（実装）** と **Gemini CLI（相談/レビュー）** の併用とする。  
※他のLLMを併用してもよいが、本ドキュメント上の役割は下記に寄せて運用する（混乱を避けるため）。

### 8.1 全体像（誰が何をするか）

- **人間（最終判断者）**: 目的/制約/優先順位の決定、受入条件の確定、最終レビューとGO判断。
- **Codex（実装担当）**: 実装、リファクタ、テスト、ビルド、差分（PR/コミット）作成、ドキュメント追記。
- **Gemini CLI（相談・レビュー担当）**: 設計相談、落とし穴/リスク洗い出し、代替案提示、レビュー観点（テスト観点含む）の提示。

### 8.2 運用ルール（Codexが守ること）

- 設計の不確実性が高い場合は、実装を急がず **Gemini CLIに相談すべき論点**（選択肢/判断基準/未確定事項）を短く提示する。
- UI/UXに影響が大きい変更（画面構成、配色、操作フロー、ショートカット体系）は、**Gemini CLIでのレビュー前提**として差分と意図を明確にする。
- いずれのAI利用でも、プロダクトの制約（外部通信なし、裁量排除、監査性など）は必ず優先し、要件と矛盾する提案は採用しない。

### 8.3 相談のトリガ（迷ったらここを使う）

- **Gemini CLIへ相談**: 要件が曖昧／複数解釈がある、重大な設計変更、監査ログ/改ざん防止の方式、ルールセット固定の設計、性能KPIの定義。
- **Gemini CLIへ相談（UI/UX）**: 3ペインの情報設計、色/コントラスト/ハイライト、フォーカス遷移、ショートカット体系、ダークモード方針、長時間作業の疲労軽減。
- **Codexで実装を進める**: 仕様が確定しているUI実装、データモデル、入出力、ログ/CSV、テスト、ビルド手順、ドキュメント整備。

### 8.4 AI利用時の成果物（最低限）

- **「何を決めたか」**（採用案/非採用案/理由）を短く残す（PR本文、または `doc/` に追記）。
- UI/UX変更は、**操作手順（ショートカット含む）**と**制約（NG-A/QLT-05等）**がどう担保されるかを明記する。
- PoC/プロトタイプでは「動くこと」だけでなく、`doc/04_技術/プロトタイプ計画.md` の観点で **評価可能な形**（ログ/出力/再現手順）にする。

### 8.5 依頼の書き方（最短で進めるための情報）

- 目的（何を良くしたいか）と受入条件（Doneの定義）
- 対象の画面/機能/NGコード（例: `QLT-05` / `STR-01S`）
- 入力データの前提（PDF/JPEG、ページ規模、例外パターン）
- 影響してよい範囲（UIだけ/ログ形式も変更可/既存互換維持 など）

### 8.6 最小運用（VS Codeだけで完結：2ターミナル）

前提：難しい仕組み（MCP / Cloud Agent / Worktree 等）は扱わず、VS Codeのターミナル2本で回す。

1. VS Codeでターミナルを2つ開く（どちらもリポジトリルート `./` をカレントにする）
2. ターミナルAで `codex` を起動（実装・修正・テスト・差分作成）
3. ターミナルBで `gemini` を起動（相談・レビュー・別案・リスク）

使い分け例（迷ったら）
- Codexへ：コード変更、リファクタ、テスト追加/修正、`dotnet build` が通るまでの対応、差分の作成と整理
- Gemini CLIへ：設計の妥当性、落とし穴、要件と矛盾していないか、代替案、レビュー観点（テスト/性能/運用）

注意
- 検品ツール本体は「外部通信なし」が制約。AIツールは開発時の相談・レビュー用途に限定し、運用/本番環境での実行を前提にしない。
- 案件データ/個人情報など、外部に出せない情報はAIへ貼らない（必要なら匿名化/ダミー化して相談する）。

### 8.7 推奨ワークフロー（迷わない手順）

1. 人間が「やりたいこと」を短く整理（目的/制約/受入条件）
2. Codexに実装依頼（作業範囲、触ってよいファイル、禁止事項）
3. Gemini CLIにレビュー依頼（差分の懸念点、別案、テスト観点）
4. 指摘を踏まえてCodexで修正（必要なら再レビュー）
5. 人間が最終確認（実行手順、簡易テスト、受入条件の充足）

### 8.8 コピペ用テンプレ（短いもの）

Codex向け（実装指示）

```text
【目的】
（例）○○を改善して△△できるようにしたい

【制約・前提】
- 外部通信なし（アプリ実行時）
- NG-Aは例外承認不可（特にQLT-05は再スキャンのみ）

【受入条件（Done）】
- （例）画面Aで××がキーボードだけで完了する
- （例）`dotnet build` が通る

【作業範囲】
- 触ってよい: （例）`src/...` と `doc/...`
- 触らない: （例）`project/...` の既存資産、出力形式の破壊的変更 など

【補足】
- 既存の章立て/設計方針（MVVM等）を尊重して最小変更で
- 変更したファイルと確認コマンドを最後に列挙して
```

Gemini CLI向け（レビュー指示）

```text
あなたは設計レビュー担当です。以下をレビューしてください。

【対象】
- 変更差分: （ここにPR/コミット概要、または変更点の要約）
- 関連資料: `doc/...`（必要最小限）

【確認してほしい観点】
1) 要件/制約（外部通信なし、裁量排除、NG-A例外不可等）に違反していないか
2) 代替案（より単純/安全/拡張しやすい案）がないか
3) 落とし穴（性能、UI操作性、ログ/監査、運用）
4) テスト観点（どこをどう確認すべきか）

【出力】
- 懸念点（重要度順）と、修正提案（具体的に）
```

---

## 9. Codexスキル運用（必要時のみ）

- ユーザーがスキル名（`$SkillName` または平文）を指定した場合、または依頼がスキルの説明に明確に合致する場合は、そのスキルの `SKILL.md` に従う。
- 参照は「必要最小限」。関連フォルダを丸ごと読み込まず、リンク先を必要な分だけ読む。
- スキル作成/更新が目的なら `skill-creator`、スキルのインストールが目的なら `skill-installer` を優先。

---

## 10. 進め方（Git / PRのベストプラクティス）

- 作業は原則ブランチで行い、`main` へはPRでマージする。
- PRには最低限以下を含める。
  - Summary（何を変えたか）
  - Why（要件/課題に対して何を解決したか）
  - How to verify（ビルド/実行/確認手順）
  - 影響範囲（UI/ログ/データ形式/互換性）
- 仕様/運用統制に関わる変更は、関連ドキュメント（`doc/`）の更新を同梱する。

---

## 11. Definition of Done（最低限の品質ゲート）

- 変更内容に対応する「根拠」が `doc/` かPR本文で追える
- ローカルで `dotnet build` が通る（該当する場合）
- UI変更はショートカット/フォーカス/制約が破れていない
- ログ/CSVなどの出力形式変更は、サンプルと読み手（運用/監査）観点の説明がある
- 外部通信を増やしていない（実行時）
