# KENPINTOOL — AGENTS.md (Python Edition)

本リポジトリでCodex（コーディング支援エージェント）が作業する際の、前提・方針・優先順位を定義します。

---

## 1. プロジェクト概要

- 契約書等の電子化業務における「検品バラツキ」を排除する自動検品ツール。
- **Python (PySide6)** をベースとしたデスクトップアプリケーション。
- **検知（AI/OpenCV）** と **確定判断（人）** を分離し、人の見落としをシステムで防ぐ。

---

## 2. 重点検知対象 (Must Have)

- **QLT-05 (色線/スジ)**: スキャナ起因の垂直方向のノイズ。
- **QLT-01 (角折れ)**: 書類の隅が折れ、内容が隠れている状態。
- **STR-01/02 (重複・抜け)**: ページの重複や欠落の疑い。

---

## 3. 実装の方針

- **UI/UX**: キーボード操作完結、3ペイン（リスト・画像・判定）。
- **解析エンジン**: 
  - OpenCV を中心とした画像処理パイプライン。
  - 重い処理は別プロセス/スレッドで実行し、UIをフリーズさせない。
- **監査性**: 
  - 判定理由（OK/NG）を必ず理由コードと共にSQLite3へ記録。
  - AI判定を「OK」で上書きする際は理由入力を必須とする。

---

## 4. コーディング規約

- **Python 3.10+**: 型ヒント (Type Hints) を積極的に活用する。
- **PEP 8**: 標準的な規約に従い、読みやすい命名を心がける。
- **Docstring**: Google Style の docstring を記述する。
- **モジュール化**: `engine/` (解析), `ui/` (画面), `data/` (DB/モデル) を明確に分離する。
- **外部通信禁止**: アプリ実行時の外部通信（API等）は一切行わない。

---

## 5. 技術スタック

- **GUI**: PySide6 (Qt for Python)
- **画像処理**: OpenCV (`opencv-python`), Pillow
- **データベース**: SQLite3
- **配布形態**: PyInstaller によるexe化を想定
