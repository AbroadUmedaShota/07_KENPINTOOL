# KENPINTOOL 開発タスクリスト

本プロジェクトの完遂（v1.0）に向けたタスク一覧です。
現在は、プロトタイプのレビュー指摘事項（優先度：高）の対応を優先的に実施します。

---

## 🔥 【最優先】 プロトタイプ修正（レビュー指摘事項）
**現行実装における操作上の致命的な不備およびログ出力の改善を行います。**

### 1. 判定リセット機能（"片道切符"問題の解消）
誤って「再スキャン」等の判定を行った際に、判定前の状態（疑い状態）に戻せるようにします。これにより、操作ミスによる作業停止を防止します。

- [x] **Model: `PageItem.cs` の改修**
  - [x] `ResetDecision()` メソッドの追加（`Decision = null` へのリセット）
  - [x] **疑いコードの復元**: 昇格されたNGコード（STR-01/STR-03）を元の疑いコード（STR-01S/STR-03S）に戻す処理
  - [x] 検知リスト (`Detections`) の `IsActive` 状態を初期状態へリセット

- [x] **ViewModel: `MainViewModel.cs` の改修**
  - [x] `ResetDecisionCommand` の定義と実装（`Backspace` / `Delete` キー割り当て）
  - [x] 監査ログに「判定取り消し (`decision_reset`)」イベントを記録
  - [x] コマンド実行後に各操作ボタンの活性状態が正しく更新されることを確認

- [x] **View: `MainWindow.xaml` の改修**
  - [x] 右ペインに「判定クリア（戻す）」ボタンを追加
  - [x] ショートカットキー (`Backspace`) の登録

### 2. 監査ログの構造化
ログの可読性を向上させ、判定の根拠と解除された疑いを明確に区別できるようにします。

- [x] **ViewModel: `MainViewModel.cs` の改修**
  - [x] `AppendDecisionLog` の出力構造を以下のように変更
    - `activeNgCodes`: 最終的な判定の根拠となったNGコード
    - `resolvedNgCodes`: OK判定や例外承認によって解除（承認）されたNGコード

---

## 🏁 マイルストーン：MVP-1（検品ワークフローの完成）
**案件選択から検品、完了報告までの標準的な一連のフローを完成させます。**

### 3. 案件管理（スタート画面）
- [x] **案件一覧画面（ダッシュボード）の実装**
  - [x] DBから既存案件（実施中・完了）を取得してリスト表示する画面を作成
  - [x] 新規案件の投入（フォルダ選択 → DB登録）機能をダッシュボードへ移動
  - [x] 案件ステータス（未着手・実施中・完了）の表示
- [x] **案件の再開（Resume）機能のUI実装**
  - [x] ダッシュボードで案件を選択し、前回の判定状態から検品画面を開く機能

### 3. 案件完了（エンド画面）
- [x] **結果確認（サマリー）画面の実装**
  - [x] 検品終了後に「OK数・NG数・例外承認数」を一覧表示する
- [x] **案件完了の統制チェック**
  - [x] 未判定ページや未確定の「疑い」が存在する場合、完了をブロックする機能を実装
- [x] **最終出力の集約**
  - [x] 完了画面からCSVおよびレポートPDFを出力し、案件ステータスを「完了」に更新

### 5. 検品操作のブラッシュアップ（継続）
- [ ] **キーボード操作の完全化**
  - [ ] マウスを使用せず、すべての検品操作が完結することを確認

---

## 🚀 マイルストーン：MVP-2（自動検知の本格導入）
**検知アルゴリズムをプロトタイプ用（ダミー）から実用レベルへ移行します。**

### 6. 検知ロジックの実装（Phase 1 対象）
- [ ] **STR-01（白紙・乱丁）の本実装**
- [ ] **CUT系（画像欠け・見切れ）の検知実装**
- [ ] **QLT-01/02（ピンボケ・低解像度）の検知実装**
- [ ] **ダミー検知（DummyDetectionService）の廃止**
  - [ ] すべての検知処理を実解析ロジックへ移行

---

## 🛡️ マイルストーン：OPS-1 / 共通（運用・信頼性）
**実運用におけるセキュリティおよび配布の要件に対応します。**

### 7. 監査とセキュリティ
- [ ] **監査ログの改ざん検知実装**
  - [ ] ハッシュチェーン等によるログの整合性検証機能
- [ ] **ユーザー管理機能**
  - [ ] 管理者モード（設定変更可）と作業者モード（検品専用）の権限分離

### 8. 配布・保守
- [ ] **バックアップ・リストア機能**
  - [ ] データベースおよび設定ファイルの一括バックアップ
- [ ] **インストーラー / 配布パッケージの作成**
  - [ ] `dotnet publish` による自己完結型配布パッケージの作成

---

## ✅ 完了済み（アーカイブ）
- [x] UI基盤の成立（3ペイン・ズーム・パン）
- [x] 基本的なショートカット実装
- [x] SQLiteによる永続化の土台（バックエンドロジックのみ）
- [x] PDF/JPEGの読み込みと表示
- [x] QLT-05（線状ノイズ）の初期検知ロジック
- [x] レポートPDF / CSVの基本出力